# 核心依赖
requests>=2.31.0        # 世界模型 API / 语言模型 API 调用
numpy>=1.26.0           # 数值计算
trimesh>=4.0.0          # 网格几何解析（体素占用、连通域、包围盒）
networkx>=3.0           # 场景关系图构建
Pillow>=10.0.0          # 全景图处理与降采样
pandas>=2.0.0           # 结果汇总

# 可视化（生成 figures/ 中的插图）
matplotlib>=3.8.0

# 可选：本地语言模型 CLI（若使用本机 Codex CLI 作为调度后端）
# 无需 Python 包，通过命令行调用
